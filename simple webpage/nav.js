const pic = document.querySelector("#img1");

pic.addEventListener("click",() => {
    pic.style.transform = "rotate(20deg)"
})

const btn = document.querySelector("#log");

btn.addEventListener("click",() => {
    pic.style.transform = "rotate(20deg)"
})

